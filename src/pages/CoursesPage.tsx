// Page placeholder
const CoursesPage = () => (
  <div className="flex flex-col items-center justify-center min-h-screen">
    <p className="text-muted-foreground">CoursesPage — à implémenter</p>
  </div>
)
export default CoursesPage
